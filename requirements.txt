# Python 3.11. Versions are the ones the recorded sweeps ran on.
numpy>=2.4
pandas>=2.3
pyarrow>=25.0      # data/raw/*.parquet
openpyxl>=3.1      # prep/ reads ERCOT's .xlsx archives
matplotlib>=3.11   # charts in web/ and prep/
requests>=2.34     # prep/ data pulls only
pytest>=9.1
hypothesis>=6.168  # tests/property
