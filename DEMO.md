# DEMO.md

> **History.** This is the 3-minute pitch script from a hackathon prep sprint (2026-09-15..17).
> The hackathon was dropped on 2026-09-23. Numbers below are from the build at that time and some
> have since moved (the 2026-09-24 allocator fix: Beat B's cut is now 928 -> 145 kW, the late Notice
> says 232). `RATIONALE.md` is the current account of what the evidence shows.

**The 3:00 demo, written so a stranger can run it cold.** That is not a figure of speech — SPEC §14
schedules **RT2 at H38: a stranger runs this document cold**, without you in the room. If they get
stuck, the document is wrong, not the stranger.

**Hard rules**
- **Nothing is retuned during the event.** Every number below comes from `ASSUMPTIONS.md` or
  `scenarios/S1_sizing.md`. If a number on screen disagrees with a number here, **the number here is
  updated from the run — never the other way round** (§15).
- **The backup video exists by H40** and is the fallback for any failure in §5 below.
- Pre-computed forks only. No live compute during the 3:00.

**Provenance of every expected value below** — this matters, because most of them were computed
before the thing that produces them existed:

| Tag | Means |
|---|---|
| **[sized]** | from `scenarios/S1_sizing.md`, 400 devices, generated by `prep/size_s1.py` |
| **[practice]** | measured in the 2026-09-16 practice build at 1:8 scale (50 devices), `PRACTICE-NOTES.md` |
| **[TBD@H26]** | **not yet measured by anything.** Fill from the real run at the H26 checkpoint, before RT1 sees this file. A `[TBD@H26]` still present at H38 is a bug in the demo, not a missing detail. |

---

## 1. Pre-flight (T − 10 min)

| # | Check | Pass |
|---|---|---|
| 1 | Laptop on **mains**, power plan = high performance, sleep/screensaver off, notifications off | — |
| 2 | Display **1280×720**, browser at 100% zoom, full screen | text ≥ 48 px on hero numbers |
| 3 | `python run.py demo` — re-bakes, then opens `web/demo.html`. **There is no server**: it is a local file that loads `web/out/runs.js` with a `<script src>`. | browser opens; devtools Network shows **0 external requests** |
| 4 | `python prep/bake_runs.py --check` — the committed bake matches the checked-out code | prints `ok: ... matches the current code` |
| 5 | Keys **1, 2, 3, B** all draw, at seed **42**. **Key `4` is not in the script** — Beat C was cut; it prints a black NOT BUILT banner if pressed. | 3 runs in the bake + the batch tab (`ceiling valid`, floor breaches **0**) |
| 6 | `python run.py gate` — **12 lenses expected, 12 ran, 12 passed**, ending `GATE PASS` | a PENDING lens is a FAIL, not a pass. The `perf` lens reads `prep/out/batch_results.json`, so a stale sweep fails here rather than on stage |
| 7 | **D1 asserts green** on the demo seed | `tests/known_answer/test_d1.py` |
| 8 | Press **`R`**. Clock reads **15:00:00 CT** and the **NOW** column is all `–` | reset works |
| 9 | Backup video open in a second tab, cued to 0:00 | — |
| 10 | Slide deck open at slide 1, **static PNGs** (no live charts in slides) | — |

**If check 3, 4 or 8 fails: run the backup video.** Do not debug in front of judges.
There is no process to restart — if the page is wrong, the bake is wrong, and that is not a stage repair.

---

## 2. The 3:00

Read the **Say** column. Do the **Do** column. The **Must be on screen** column is what the demo
test asserts via the API — if it is not there, you are on the wrong run.

**Timing, measured not guessed.** The Say column is **433 spoken words** (whitespace-separated
tokens inside the quoted passages of §2's `Do | Say | On screen` rows, recounted 2026-09-17 after
per-bucket admission rewrote Beat B; `tests/demo` asserts it, so it cannot drift again). That is **2:59 at 145 wpm** and **2:53 at 150**, against the §15 bar of
≤ 3:00 — real slack, but not much. At a slow 130 wpm it is 3:18, so **rehearse with a clock**.

**If a rehearsal runs over, cut in this order** (and only this order — the first two cuts cost
nothing the pitch depends on):

1. **Beat C, the controller crash** (−34 w, ≈14 s). Move it to Q&A; nothing else references it.
2. **The lease-cost sentence in slide 3** (−18 w, ≈7 s). It has its own chart for Q&A.
3. **The second half of slide 5**, keeping only the three questions (−15 w, ≈6 s).

**Never cut:** Beat A's binding reason and the five-hour lead · the `1` toggle (that is D1, live) ·
Beat B's `reasonable` comparison · the "where we lose" line in slide 3.

### Slide 1 — "The grid's hardest hour came three hours after its biggest one" · 0:00–0:20

| Do | Say | Must be on screen |
|---|---|---|
| Slide 1 (static PNG: `prep/out/slide1_2026-07-22.png`) | "ERCOT, July 22nd. Record load at 6pm — 91 gigawatts. The *hard* hour was 9pm: record net load, 11 gigawatts from batteries. **The battery that shaved your peak may be empty at nine** — and on a bad night a fifth of your fleet is invisible." | Load peak **91.1 GW HE18:00**; net-load peak **75.2 GW HE21:00**; batteries **−0.5 GW at the load peak**, **+11.3 GW at 9pm**. All [sized] from our own EIA-930 pull. |

### Slide 2 — "Two contracts, one evening's kWh" · 0:20–1:30 (live)

**Beat A — the squeeze, five hours early (0:20–0:55)**

| Do | Say | Must be on screen |
|---|---|---|
| Switch to the app. Clock 15:00, 60×. | "Four hundred batteries in one ADER aggregation, three paid claims tonight. The co-op books a three-hour peak shave; ERCOT holds 1.5 megawatts of ancillary service for 7 to 9." | Bookings: `ADER AS 19–21 1.5 MW hold` · `CO-OP PEAK 15:30–18:30 3.0 MW` |
| Point at the ADER ENERGY row. | "The 8-to-9 energy award asks for two megawatts and gets **928 kilowatts** — not a wire limit; the kilowatt-*hours* are already spoken for. Binding reason: **kWh slack**. And it says so at 3pm. **Five hours before delivery.**" | `ADER ENERGY 20–21: 2.0 → 0.93 PARTIAL kwh_slack` **[sized]**, lead time **5 h** |
| Press **`1`** (toggle co-op off), pause, press **`1`** again. | "Take the co-op away — full two megawatts. Put it back — 928. The cut equals the co-op's energy minus the slack without it. That's a test, not a demo." | ADER ENERGY 2.0 MW ⇄ 0.93 MW; cut **1.072 MWh** **[sized]**; D1 badge green |

**Beat B — a tower goes dark mid-delivery (0:55–1:30)**

| Do | Say | Must be on screen |
|---|---|---|
| Press **`2`** (tower down 20:15). | "It's 8:15 and a cell region goes quiet — eighty batteries, still running, invisible. The ancillary hold doesn't move — it's senior. The junior award takes the cut: **763 to 120 kilowatts**, named for the 20:25 bucket, **five minutes early**." | Region R3 dark · at the 20:20 re-plan, **one** Notice: `ADER_ENERGY cut 763 -> 120 kW (reserve) from 20:25, 5 min ahead of delivery` (then 87, then 84 kW as R3 stays dark) · `ADER AS` **still 1.5 MW**, capacity availability **100%** · **silent breaches 0**, late breaches 0 |
| Switch controller to `reasonable`. | "Same night, a controller that isn't naive — it checks every claim against the energy it has, and delivers the 8-to-9 award **in full**. **What it loses is the ancillary-service hold it's paid to keep**, quietly. Over a thousand evenings it holds back *nothing* — it promises **more than ten per cent** more than physics allows, and never says so." | `reasonable`: capacity availability **50%** **[practice]** · **60 silent capacity buckets** **[practice]** · ENERGY promise kept **~100%** **[practice]** |

> ⚠️ **Nothing is seen for four minutes, and that is the point.** R3 goes dark at 20:15:00; the
> controller re-plans every five minutes, so it learns at 20:20. The reserve carries those four
> minutes, and the bucket in flight at 20:20, **in full** - so "right now is covered" is true, and
> measured (`tests/demo`). The Notice then cuts the tail from 20:25, five minutes ahead.
> (`PRACTICE-NOTES.md` FINDING-18, FINDING-22, FINDING-26.)

> ⚠️ **If asked why 120 kW and not more.** Two reasons, both measured. The four unseen minutes
> were carried by the lit regions, and that energy is gone when the tail is re-planned: start the
> same outage one minute earlier, so it is seen at once, and the cut is to **207 kW**. And the
> N-1 reserve is still held *while a region is already dark*. That is a policy choice, not an
> oversight (`ASSUMPTIONS.md` §2 `haircut`, `RATIONALE.md` A4), and it is the conservative one.

> ⚠️ **Do not say "reasonable under-delivers" on S1.** It doesn't — its energy delivery is ~100%.
> Saying it is wrong and a Base engineer will disprove it on stage. The loss is the capacity hold.
> (`PRACTICE-NOTES.md` FINDING-6.)

### Slide 3 — "Where we lose" · 1:30–2:10

| Do | Say | Must be on screen |
|---|---|---|
| Batch tab — deck PNG `prep/out/batch_where_we_lose.png`; press **`B`** for the live tab (`#batch` jumps straight to it). | "Every zero on that screen has a price next to it. Across **a thousand** seeded evenings we hold back **six per cent** of what a perfect oracle could promise — that's the cost of the honesty. And the leases cost **zero** extra messages: they ride on acks you already send." | **1,000** seeded evenings, 1k devices · held back **6.8%** (p90 **7.4%**) · `reasonable` **−13.1%** — it books past the ceiling · silent buckets **3** (one evening, three regions dark) vs **56,626** · "where we lose" panel non-empty · lease cost **0 extra msgs**, standalone ≈ **7%** |
| Stay on the batch tab; point at the zero line. | "And one we found on ourselves: a tower goes dark and the fleet delivers **more** than it sold — batteries we can't talk to keep running their last order. That surplus came out of the homeowner's reserve. Our own harness caught it; a demo never would." | FINDING-24/25 · seed 52: **3,000 → 3,600 kW** at 18:15, exactly the dark region's share, delivered twice · floor breaches **96 / 1,000 seeds → 0** · fix: lease expiry (SPEC §6.6) + net stranded output out of the re-spread |

> The "where we lose" panel must not be empty. A demo with no losing case is a demo nobody believes.

### Slide 4 — "The optimizer proposes. This admits." · 2:10–2:35

| Do | Say |
|---|---|
| Slide 4. | "None of the pieces are new — bands, leases, fencing, N−1, admission control. You probably have all of them. What's here is the **composition**, plus the contract on top: your optimizer proposes, this admits what the fleet can still keep, and tells the on-call engineer *early* when it can't." |

### Slide 5 — "What we'd check against real telemetry" · 2:35–3:00

| Do | Say |
|---|---|
| Slide 5. | "Three things we'd want real data for: how stale telemetry actually gets; whether the co-op-versus-ADER collision is settled in code or in a contract; and which broken rule actually pages someone. Those change the ladder, the partitions, and what counts as a Notice." |

---

## 3. The five slide titles

1. **The grid's hardest hour came three hours after its biggest one**
2. **Two contracts, one evening's kWh** *(live)*
3. **Where we lose**
4. **The optimizer proposes. This admits.**
5. **What we'd check against real telemetry**

Slides carry **static PNGs only**. Nothing in the deck renders live.

---

## 4. Q&A spares (not in the 3:00)

| Key | Shows | One-line answer it supports |
|---|---|---|
| `3` | delayed telemetry | "The band widens with data age; promises come off its pessimistic edge." |
| `5` | duplicate storm | "Store-and-forward floods dedupe on max (epoch, seq) — I5, asserted at scale." |
| — | **controller crash** (was Beat C, cut 2026-09-17) | "Epoch bumps, the log replays, devices reject stale commands. **S3 is scheduled, not built** — say that, don't bluff it. It was twenty seconds proving something you already assume; the batch finding earned the time instead." |
| — | **why the reserve costs 6%** | "That is the band plus the N−1 hold. The oracle ceiling is *searched for*, not assumed — the largest booking it can make and still keep, bisected per seed." |
| — | assumptions tab | "Every knob, its default and its range, in `ASSUMPTIONS.md`. The PARTIAL band is one cell wide at 3 h, and we say so." |
| — | §6.3 note | "We found a hole in our own ledger in prep: read one sentence the natural way and we'd admit a claim that silently kills another. It's fixed, and a planted-bug control now fails the build if it comes back." |

---

## 5. Failure playbook — each ≤ 10 s, then move on

| If | Do | Say |
|---|---|---|
| App won't load / blank chart | Backup video tab, play from 0:00 | "I'll show you the recording — same run, same seed." |
| A key does nothing | Press **`R`**, then the key once more. Still nothing → video | — |
| Numbers differ from this file | **Read what's on screen**, note it, move on | "That's the run talking — I'll reconcile it after." |
| Clock runs past the beat | Skip to the next **Do** row. Never skip Beat B | — |
| A judge interrupts mid-beat | Answer, then resume at the **start of the current beat**, not where you stopped | — |
| Projector is dim / low contrast | Light theme is already the default; raise the browser zoom one step | — |

**Never:** debug live · edit a knob to make a number appear · apologise for a losing case.

---

## 6. RT2 — the cold run at H38

Hand this file and a laptop to someone who has not seen the project. They run §1 and §2 alone.
Record, for each of the 10 pre-flight checks and each beat: **did it happen without asking you?**

Fail conditions, any one of which means the document gets rewritten before the freeze at 10:00:
- a check in §1 that cannot be performed with what is written
- a `[TBD@H26]` still on the page
- a number in §2 that disagrees with the screen
- the run takes **> 3:20** or **< 2:30** read at normal pace
- the stranger asks "what am I looking at?" during Beat A

---

## 7. What the demo test asserts (`tests/demo/`, `tests/perf/`)

Driven **in process**, not through an HTTP API — there is no server (SPEC §13 v0.7). Every "must
be on screen" cell above is read out of the same `run_scenario` / `Ledger` the gate drives, and
out of the artifact the page actually loads, so a number that stops being true fails the build
instead of failing on stage.

```
S1, seed 42, headroom + reasonable
  headroom   ADER_ENERGY  PARTIAL, reason "kwh_slack"   # not kw_room, not feeder
  headroom   ADER_ENERGY  admitted 928 +/- 10 kW        # [sized]
  headroom   lead to delivery            >= 5 h
  headroom   silent_breach_buckets       == 0
  headroom   capacity_availability_pct   == 100
  reasonable capacity_availability_pct   <  90          # the S1 miss
  reasonable silent_capacity_buckets     >  0           # NOT silent_energy_buckets
  D1: key 1 flips ADER_ENERGY to ADMITTED 2,000 kW, and
      cut_kwh == clip(coop_kwh - slack_without, 0, requested_kwh) +/- 1.0    # §8.1

S2 (R3 dark 20:15), headroom
  silent_breach_buckets       == 0
  late_breach_buckets         == 0      # the unseen minutes and the in-flight bucket are covered
  median_lead_time_s          == 300    # one full bucket, never 0
  Notices only "5 min ahead of delivery", none late: 763 -> 120 -> 87 -> 84 kW
  ADER_AS still 1,500 kW ADMITTED       # the ladder holds
  started buckets are never rewritten   # the lock invariant, read off the profile

the surface
  web/out/runs.json is NOT stale        # rebuilt and compared, every run
  web/demo.html makes no network request
  DEMO.md §2 is 431 spoken words        # the timing claim, asserted
  DEMO.md pre-flight names the real lens count, read from tools/gate.py
  Slide 3 quotes prep/out/batch_results.json and not a memory of it

the sweep (tests/perf, 1,000 seeded evenings)
  the recorded sweep still describes this code   # 3 seeds re-run and compared
  the oracle is a ceiling it actually reached    # else every held-back % is inflated
  headroom silent seeds                 <= {60}  # named, not a threshold
  any residual silence was unforeseeable         # fault inside a claim's final bucket
  floor breaches                        == 0     # FINDING-24/25, closed
  reasonable still fails the way the pitch says it does
```

**Not asserted, because not built:** S3 (controller crash/recovery). Beat C was cut on
2026-09-17 and the answer moved to §4. **Do not claim a `crash_recovery_s` number.**


## 8. Assets and versions

| Asset | Path | Frozen at |
|---|---|---|
| Slide 1 chart | `prep/out/slide1_2026-07-22.png` | ✅ 09-15 |
| Energy-left reference | `prep/out/practice_energy_left.png` (practice build) | ✅ 09-16 |
| Batch PNGs | `prep/out/batch_*.png` | H26 |
| Backup video | `prep/out/demo_backup.mp4` | **H40, hard deadline** |
| Demo seed | **42** | H2 |
| Contracts | `git tag contracts-v1` | H2 |

Every asset is local. Nothing in the demo path fetches anything.
